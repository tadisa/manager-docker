# Manager Docker Image

This image builds Manager with all Chromium dependencies required for PuppeteerSharp PDF generation.
Deploy via Coolify by pointing to this repo and selecting "Dockerfile" as the build method.
This is a old container from the days of Coolify that is not working. Nothing inside.